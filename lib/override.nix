# Intra-eval incremental override (design §9; DP3/DP6 topological reverse cone).
# STUB (Task 0): shaped placeholder; fleshed out in Task 7/8.
{
  scope,
  graph,
  rebuild,
}:
{
  override = ctx: _args: ctx;
  warmResolve = ctx: _args: ctx;
}
